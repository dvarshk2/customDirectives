import { InoutFormatDirective } from './inout-format.directive';

describe('InoutFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new InoutFormatDirective();
    expect(directive).toBeTruthy();
  });
});

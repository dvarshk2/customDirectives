import { BetterBgChangeDirective } from './better-bg-change.directive';

describe('BetterBgChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterBgChangeDirective();
    expect(directive).toBeTruthy();
  });
});

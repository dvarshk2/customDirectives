import { DebitCardDirective } from './debit-card.directive';

describe('DebitCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DebitCardDirective();
    expect(directive).toBeTruthy();
  });
});

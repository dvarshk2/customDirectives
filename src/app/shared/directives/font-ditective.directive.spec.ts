import { FontDitectiveDirective } from './font-ditective.directive';

describe('FontDitectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FontDitectiveDirective();
    expect(directive).toBeTruthy();
  });
});
